-- Priority message delivery (Premium feature)
-- Priority levels: 'normal' (default), 'high', 'urgent'
-- Urgent messages bypass quiet hours and DND
-- Column already applied; no-op to mark migration as tracked
SELECT 1;
