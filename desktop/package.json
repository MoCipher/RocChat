{
  "name": "rocchat-desktop",
  "version": "1.0.0",
  "private": true,
  "description": "RocChat Desktop — Tauri-wrapped secure messenger",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "tauri": "tauri"
  },
  "dependencies": {},
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
